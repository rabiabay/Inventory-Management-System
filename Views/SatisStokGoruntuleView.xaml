<UserControl x:Class="StokYonetimSistemi.Views.SatisStokGoruntuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="Auto" Width="Auto">
    <Grid Margin="20">
        <DataGrid x:Name="dgSatisStoklar" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ürün Adı" Binding="{Binding Urun.UrunAdi}" Width="*"/>
                <DataGridTextColumn Header="Barkod" Binding="{Binding Urun.Barkod}" Width="150"/>
                <DataGridTextColumn Header="Miktar" Binding="{Binding Miktar}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
