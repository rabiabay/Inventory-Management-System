<UserControl x:Class="StokYonetimSistemi.Views.FaturaGecmisiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <DataGrid x:Name="dgFaturalar" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Fatura ID" Binding="{Binding FaturaID}" Width="*" />
                <DataGridTextColumn Header="Sipariş ID" Binding="{Binding SiparisID}" Width="*" />
                <DataGridTextColumn Header="Tarih" Binding="{Binding Tarih}" Width="*" />
                
                <DataGridTextColumn Header="Kullanıcı ID" Binding="{Binding KullaniciID}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>


    </Grid>
</UserControl>
