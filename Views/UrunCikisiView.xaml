<UserControl x:Class="StokYonetimSistemi.Views.UrunCikisiView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Ürün:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center"/>
        <ComboBox x:Name="cbUrun" Grid.Row="0" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Miktar:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtMiktar" Grid.Row="1" Grid.Column="1" Margin="5"/>

        <TextBlock Text="Açıklama:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center"/>
        <TextBox x:Name="txtAciklama" Grid.Row="2" Grid.Column="1" Margin="5"/>

        <Button Content="Ürün Çıkışı Yap" Grid.Row="3" Grid.ColumnSpan="2" Margin="5" Padding="10"
                Click="BtnUrunCikisi_Click" HorizontalAlignment="Center"/>
    </Grid>
</UserControl>
