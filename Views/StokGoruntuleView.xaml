<UserControl x:Class="StokYonetimSistemi.Views.StokGoruntuleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1000">

    <Grid Margin="20">
        <DataGrid x:Name="dgStoklar" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ürün Adı" Binding="{Binding UrunAdi}" Width="*"/>
                <DataGridTextColumn Header="Kategori" Binding="{Binding KategoriAdi}" Width="*"/>
                <DataGridTextColumn Header="Depo" Binding="{Binding DepoAdi}" Width="*"/>
                <DataGridTextColumn Header="Toplam Miktar" Binding="{Binding Miktar}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
