<UserControl x:Class="StokYonetimSistemi.Views.FavoriUrunlerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:StokYonetimSistemi"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="20">
        <DataGrid x:Name="dgFavoriler" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Ürün Adı" Binding="{Binding UrunAdi}" Width="*"/>
                <DataGridTextColumn Header="Barkod" Binding="{Binding Barkod}" Width="*"/>
                <DataGridTextColumn Header="Açıklama" Binding="{Binding Aciklama}" Width="*"/>
                <DataGridTextColumn Header="Birim" Binding="{Binding Birim}" Width="*"/>
                <DataGridTemplateColumn Header="İşlem" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Sipariş Et"
                                    Click="BtnSiparisEt_Click"
                                    Tag="{Binding}" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
